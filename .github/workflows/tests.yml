name: test

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ["3.9", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - run: pip install -e .[dev]
      - run: pytest -q
      - run: pip install -e ./pytest-hwcontract --no-deps
      - run: pytest pytest-hwcontract/tests -q
      - run: python3 -m hwcontract.server --selftest
      - run: python3 -m hwcontract.judge --demo
      - run: python3 -m hwcontract.sigrok_adapter --demo
      - run: python3 -m hwcontract.serial_adapter --demo
